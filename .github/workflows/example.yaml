name: Hello World in Container

on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest
  

    steps:

    - name: Run Hello World in Docker#
      
      run: |
        apt update >> /dev/null
        apt install -y curl dmidecode >> /dev/null
        /usr/sbin/dmidecode -s system-manufacturer | tr "[:upper:]" "[:lower:]" | grep -q "microsoft corporation"



